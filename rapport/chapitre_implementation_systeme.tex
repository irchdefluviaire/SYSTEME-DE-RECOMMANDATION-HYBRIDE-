\chapter[CONSTRUCTION DU SYSTÈME HYBRIDE EMPLOI--COMPÉTENCES]{CONSTRUCTION OPÉRATIONNELLE DU SYSTÈME HYBRIDE EMPLOI--COMPÉTENCES}
Ce chapitre suit le workflow réellement implémenté : préparation des données, adaptation de l'encodeur, indexation dans pgvector, construction du graphe Neo4j et calcul du score hybride. Seuls les résultats nécessaires pour justifier ces choix techniques sont présentés ; leur évaluation comparative est réservée au chapitre~4.

\section{Données exploitables et contraintes de qualité du système}
\subsection{Volumes disponibles après nettoyage}

L'ETL produit les offres, profils et référentiels normalisés qui alimentent l'encodeur, pgvector et Neo4j. Le corpus compte 13\,957 offres pour 41\,298 candidats, soit environ trois profils pour une offre. Ce ratio ne prouve pas mécaniquement un biais : le risque apparaît lorsqu'un échantillonnage de couples candidat--offre ou un voisinage vectoriel expose davantage les secteurs, intitulés ou compétences les plus fréquents. Ces signaux de popularité peuvent alors masquer des différences utiles entre profils. Le rappel sémantique est donc suivi d'un contrôle relationnel qui vérifie les compétences requises plutôt que la seule fréquence ; les volumes complets figurent au tableau~\ref{tab:implementation_volumes_exploites} en annexe.

Les principales opérations appliquées sont :
\begin{itemize}
  \item harmonisation de la casse, des espaces, des accents et des formats de texte ;
  \item suppression prudente des doublons strictement identiques ;
  \item normalisation des localisations, secteurs, niveaux et types de contrat ;
  \item conservation explicite des valeurs manquantes, sans inventer d'information ;
  \item composition d'un texte exploitable à partir du titre, des compétences et des métadonnées disponibles lorsque la description est insuffisante.
\end{itemize}
Les offres proviennent de plateformes scrapées et de sources institutionnelles. Louma-Jobs représente 43,14\,\% du corpus : les résultats peuvent refléter les caractéristiques de cette source et ne décrivent pas exhaustivement le marché camerounais de l'emploi.
\begin{figure}[H]
  \centering
  \caption{Répartition des offres par source après nettoyage}
  \label{fig:implementation_sources_offres}
  \includegraphics[width=0.90\textwidth]{figures/generated/implementation_deep_current/01_offres_par_source.png}
  \source{Nos travaux, traitements Python de nettoyage et de diagnostic}
\end{figure}
\subsection{Qualité des variables utiles au matching emploi-compétences}
Le matching exploite la description, les compétences, la localisation, le secteur et le niveau. La source reste une variable de traçabilité et permet d'interpréter les écarts de qualité entre plateformes.

\subsubsection{Localisation exploitable pour filtrer sans exclure}
La localisation est utilisée comme filtre souple : 79,88\,\% des offres sont rattachées au Cameroun et 20,12\,\% à l'international. Une ville absente n'est pas imputée ; le pays disponible est conservé. Le diagnostic détaillé figure en annexe (figure~\ref{fig:implementation_localisation}).

\subsubsection{Compétences et descriptions : signal utile mais hétérogène}

\begin{figure}[H]
  \centering
  \caption{Compétences fréquentes et disponibilité descriptive}
  \label{fig:implementation_competences_descriptions}
  \includegraphics[width=0.90\textwidth]{figures/generated/implementation_deep_current/03_competences_descriptions.png}
  \source{Nos travaux, traitements Python de nettoyage et de diagnostic.}
\end{figure}

Les libellés fréquents mélangent compétences observables et domaines d'activité ; ils sont utilisés comme indices de rapprochement, non comme preuve de maîtrise. Louma-Jobs ne fournit une description que pour 4,90\,\% de ses offres. Pour éviter qu'une description absente produise un vecteur pauvre, le texte encodé combine titre, compétences, secteur, localisation et autres métadonnées disponibles.

\subsection{Nettoyage réalisé et limites conservées}

Le nettoyage harmonise les formats sans inventer les valeurs absentes. Les localisations suivent la priorité ville--pays et les doublons potentiels sont audités sans suppression automatique, car deux annonces proches peuvent correspondre à des publications distinctes. Les règles détaillées figurent au tableau~\ref{tab:implementation_nettoyage_donnees} en annexe.

Le pipeline peut traiter de nouvelles offres et de nouveaux profils dès lors que les colonnes minimales sont respectées. Une nouvelle exécution applique les mêmes contrôles, régénère les fichiers normalisés, puis permet de réencoder et synchroniser les nouvelles entités dans les bases de données. Il s'agit d'une actualisation par lots contrôlée, non d'une ingestion en temps réel ; un changement du format d'une source scrapée exige d'adapter son mapping.

\section{Adaptation du modèle d'embeddings au domaine emploi--compétences}

Après l'ETL, les textes nettoyés servent à adapter l'encodeur avant leur indexation dans pgvector. L'objectif est d'améliorer le classement sémantique, non de générer une réponse.

La logique de cette section se résume en quatre étapes. Un encodeur compact est d'abord retenu pour représenter les textes ; 6\,476 paires offre--description servent ensuite à l'apprentissage contrastif par \textit{MultipleNegativesRankingLoss} ; le checkpoint est choisi à partir des métriques de validation ; enfin, les embeddings du modèle retenu alimentent l'index vectoriel. Les courbes, le benchmark et la projection qui suivent documentent chacune une étape de cette chaîne. Elles constituent des diagnostics du prototype, non une preuve directe de pertinence métier.
\subsection{Encodeur retenu pour les paires contrastives}
\begin{table}[H]
	\centering
	\caption{Protocole de construction des paires de fine-tuning}
	\label{tab:implementation_protocole_finetuning}
	\tableStyleStart
	\begin{tabularx}{\textwidth}{p{3.5cm}X}
		\rowcolor{darkblue}
		\tabheader{Élément} & \tabheader{Implémentation observée} \\
		\midrule
		Modèle de départ & \texttt{sentence-transformers/all-MiniLM-L6-v2}, embeddings denses de 384d. \\
		Rôle de \texttt{sentence1} & titre, secteur, contrat, niveau d'études, expérience, ville et compétences. \\
		Rôle de \texttt{sentence2} & Détails nettoyés de l'annonce uniquement, c'est-à-dire le texte descriptif que le modèle doit rapprocher de la représentation structurée. \\
		Filtrage informatif & Conservation des paires avec compétences disponibles, \texttt{sentence1} d'au moins 40 caractères et \texttt{sentence2} d'au moins 120 caractères ; le champ \texttt{ft\_eligible} est appliqué lorsqu'il existe. \\
		Total de paires & 6\,476 paires offre-description. \\
		%Split & 4\,542 paires en train, 981 en validation et 953 en test, avec stratification par secteur principal. \\
		%Fichiers produits & \filevar{data/finetune/pairs\_train.jsonl}, \filevar{pairs\_val.jsonl}, \filevar{pairs\_test.jsonl} et \filevar{pairs\_metadata.json}. \\
		\bottomrule
	\end{tabularx}
	\tableStyleEnd
	\source{Nos travaux, traitements Python et SentenceTransformer.}
\end{table}
Le modèle \texttt{all-MiniLM-L6-v2} a été retenu pour ses vecteurs de 384 dimensions, moins coûteux à encoder, stocker et rechercher que des représentations plus larges. 
Son fine-tuning adapte cet espace généraliste aux intitulés, secteurs, niveaux et compétences du corpus local sans modifier l'architecture de production.


\texttt{sentence1} contient la représentation structurée de l'offre et \texttt{sentence2} sa description. Les seuils de 40 et 120 caractères excluent les paires trop pauvres pour fournir un signal d'apprentissage exploitable.
\subsection{Fine-tuning : contraintes de calcul et preuve de gain}

\subsubsection{Paramétrage contraint et dynamique d'apprentissage}

\begin{table}[H]
  \centering
  \caption{Hyperparamètres du fine-tuning SentenceTransformer}
  \label{tab:implementation_hyperparametres_finetuning}
  \tableStyleStart
  \begin{tabularx}{\textwidth}{p{3.5cm}p{2.4cm}X}
    \rowcolor{darkblue}
    \tabheader{Hyperparamètre} & \tabheader{Valeur} & \tabheader{Justification} \\
    \midrule
    Nombre maximal d'époques & 5 & Plafond court retenu sous contrainte CPU pour observer la stabilisation sans prolonger inutilement l'entraînement. Le meilleur checkpoint apparaît autour de l'époque~4 ; la cinquième époque confirme le léger recul. \\
    Taille de lot & 32 & Compromis entre mémoire disponible, temps de calcul et diversité des exemples comparés dans chaque lot. \\
    Négatifs implicites & 31 par exemple & Avec un lot de 32 paires, les 31 autres descriptions servent de contre-exemples sans créer un fichier supplémentaire de négatifs. \\
    Taux d'apprentissage maximal & $2\times10^{-5}$ & Valeur prudente pour adapter le modèle préentraîné sans modifier brutalement ses représentations initiales. \\
    Calendrier du taux & Cosinus avec warmup & Augmentation progressive au démarrage, puis diminution contrôlée afin de stabiliser la fin de l'entraînement. \\
    Warmup & 100 étapes au maximum & Limite les mises à jour instables au début ; le nombre effectif reste borné par les étapes disponibles. \\
    Fonction de perte & \makecell[l]{\textit{MultipleNegatives}\\\textit{RankingLoss}} & Adaptée au retrieval : elle rapproche chaque paire correcte et éloigne les autres éléments du lot selon la similarité cosinus. \\
    \bottomrule
  \end{tabularx}
  \tableStyleEnd
  \source{Nos travaux}
\end{table}

Ces choix répondent aux ressources disponibles. L'entraînement a duré 15\,160 secondes sur CPU. Cinq époques ne signifient pas que la cinquième est la meilleure : elles constituent une borne d'observation, tandis que le checkpoint retenu est celui qui obtient la meilleure validation autour de l'époque~4.

La perte d'entraînement diminue de 2,445 à 0,132, tandis que la perte de test reste plus élevée (0,433) que celle d'entraînement (0,171). Cet écart signale une généralisation moins bonne sur les paires non vues, sans divergence du modèle. Le panneau des métriques montre un NDCG@10 maximal à l'époque~4, puis un léger recul ; le panneau du learning rate confirme une montée puis une décroissance contrôlée du pas d'apprentissage. Prolonger l'entraînement n'était donc pas justifié.
\begin{figure}[H]
  \centering
  \caption{perte, généralisation, métriques de validation et calendrier du learning rate}
  \label{fig:implementation_finetuning_courbes}
  \includegraphics[width=0.92\textwidth]{figures/generated/implementation_deep_current/04_finetuning_courbes.png}
  \source{Nos travaux, traitements Python et SentenceTransformer.}
\end{figure}
\subsubsection{Preuve de gain sur le ranking sémantique}
Sur 953 paires de test, le fine-tuning multiplie le NDCG@10 par 3,7 et le MRR@10 par 4,2 par rapport au modèle de départ. La notation @10 signifie que le calcul examine les dix premiers résultats retournés pour chaque requête. Le NDCG vérifie leur ordre, tandis que le MRR dépend de la position du premier résultat pertinent. Le détail figure au tableau~\ref{tab:implementation_baseline_vs_finetuned} en annexe ; l'évaluation complète est présentée au chapitre~4.

Le modèle fine-tuné obtient un NDCG@10 de 0,6232, un MRR@10 de 0,5874 et un Recall@10 de 0,7398. Les barres de qualité de classement et de rappel justifient son choix pour pgvector ; la latence rappelle toutefois que le meilleur modèle statistique doit rester compatible avec une recherche interactive. TF--IDF reste un complément rapide pour les correspondances lexicales.
\begin{figure}[H]
	\centering
	\caption{Comparaison des modèles d'embeddings : qualité de classement, rappel et latence}
	\label{fig:implementation_benchmark_embeddings}
	\includegraphics[width=0.92\textwidth]{figures/generated/implementation_deep_current/05_benchmark_modeles_embeddings.png}
	\source{Nos travaux, traitements Python et SentenceTransformer}
\end{figure}
\subsection{Structure de l'espace vectoriel après fine-tuning}
La projection porte sur 1\,810 embeddings : 350 offres, 350 candidats, 350 métiers, 350 compétences, 209 groupes \MEPC{} et 201 domaines \NCF{}. Le noyau central mélange plusieurs catégories. Cette absence de séparation stricte est utile au matching : le modèle organise principalement les textes selon leur proximité de sens plutôt que selon leur type administratif. Un candidat peut ainsi se retrouver près d'une offre, d'un métier ou d'une compétence décrivant le même domaine professionnel.
\begin{figure}[H]
  \centering
  \caption{Projection UMAP des embeddings indexés}
  \label{fig:implementation_espace_vectoriel}
  \includegraphics[width=0.94\textwidth]{figures/generated/implementation_deep_current/07_espace_vectoriel_embeddings.png}
  \source{Nos travaux, traitements Python et SentenceTransformer}
\end{figure}
Les offres sont toutefois plus dispersées et forment plusieurs prolongements autour du noyau. Leurs textes combinent davantage d'informations -- intitulé, missions, contraintes, secteur et localisation -- ce qui produit des représentations plus variées que les libellés généralement courts des métiers ou des compétences. Les petits groupes éloignés peuvent correspondre à des domaines spécialisés, à des formulations rares ou à des descriptions peu renseignées. Ils doivent être contrôlés, mais leur éloignement ne prouve pas à lui seul une erreur d'encodage.

La figure montre donc que l'espace permet des voisinages entre objets de nature différente, condition nécessaire au retrieval emploi--compétences. Elle ne démontre cependant pas que chaque voisin est une bonne recommandation. UMAP réduit les vecteurs de 384 dimensions à deux dimensions et privilégie les voisinages locaux ; les axes UMAP~1 et UMAP~2 n'ont pas de signification métier directe. La qualité finale est vérifiée par les métriques de classement, puis par les relations Neo4j utilisées pour confirmer ou corriger la proximité sémantique. Une projection t-SNE pourrait compléter ce diagnostic : elle met souvent davantage en évidence les groupes locaux, mais dépend fortement de la perplexité, du taux d'apprentissage, du nombre d'itérations et de la graine aléatoire. UMAP et t-SNE doivent donc être lus comme des visualisations exploratoires convergentes, jamais comme une mesure de performance ou une preuve de séparation métier.
%\begin{figure}[H]
%  \centering
%  \includegraphics[width=0.94\textwidth]{figures/generated/implementation_deep_current/06_pgvector_retrieval_dashboard.png}
%  \caption{Dashboard pgvector : couverture, synchronisation graphe, latence et index}
%  \label{fig:implementation_pgvector_retrieval_dashboard}
 % \source{\texttt{06\_pgvector\_counts.csv}, \texttt{06\_pgvector\_latency\_summary.csv}, \texttt{06\_pgvector\_sizes.csv}, \texttt{06\_pgvector\_indexes.csv} et \texttt{06\_pgvector\_doc\_chunks.csv}}
%\end{figure}

%Ce graphique ne répète pas seulement les volumes du tableau suivant. Il donne quatre informations opérationnelles différentes : la taille du vivier vectorisé, la capacité à rejoindre Neo4j, la latence du top-10 HNSW et les familles d'index disponibles. La lecture est simple : pgvector couvre les entités utiles, toutes sont raccordables au graphe, la médiane de recherche reste autour de 31 ms, mais le maximum observé rappelle qu'une application interactive doit surveiller les cas lents. Le graphique sert donc à juger la brique comme outil de retrieval, pas comme simple base de stockage.

\section{Mémoire vectorielle pgvector pour le retrieval}

Après l'encodage, pgvector récupère un top-$K$ d'objets sémantiquement proches. Ce vivier est ensuite contrôlé et rerangé par Neo4j.
\subsection{Schéma physique, tables et volumes indexés}
La table \texttt{embeddings} stocke les entités structurées ; \texttt{doc\_chunks} contient les fragments des référentiels. Le schéma montre aussi le rôle des index : accélérer la similarité vectorielle, la recherche textuelle et les filtres par type d'entité. L'identifiant commun assure ensuite le passage vers Neo4j.
\begin{figure}[H]
  \centering
  \caption{Schéma physique de la base pgvector}
  \label{fig:implementation_schema_pgvector}
  \includegraphics[width=0.90\textwidth]{pgvector.png}
  \source{Nos travaux, diagnostics PostgreSQL--pgvector}
\end{figure}
L'instance contient 72\,643 embeddings d'entités et 142 embeddings documentaires, tous en 384 dimensions. Les identifiants communs permettent de replacer les résultats vectoriels dans le graphe afin de récupérer leurs relations métier. Cette continuité est un lien technique ; elle ne constitue pas, à elle seule, une preuve de qualité sémantique.
\begin{table}[H]
  \centering
  \caption{Distribution des embeddings par type d'entité}
  \label{tab:implementation_pgvector_distribution}
  \tableStyleStart
  \begin{tabularx}{0.92\textwidth}{Xrr}
    \rowcolor{darkblue}
    \tabheader{Type d'entité} & \tabheader{Vecteurs} & \tabheader{Part} \\
    \midrule
    Candidats & 41\,298 & 56,85\,\% \\
    Offres d'emploi & 13\,957 & 19,22\,\% \\
    Compétences & 13\,939 & 19,19\,\% \\
    Métiers & 3\,039 & 4,18\,\% \\
    Groupes de base MEPC & 209 & 0,29\,\% \\
    Domaines détaillés NCF & 201 & 0,28\,\% \\
    \midrule
    Total entités structurées & 72\,643 & 100\,\% \\
    Chunks documentaires & 142 & -- \\
    \bottomrule
  \end{tabularx}
  \tableStyleEnd
  \source{Nos travaux, diagnostics PostgreSQL-pgvector.}
\end{table}
\subsection{Référentiels découpés en chunks interrogeables}

Les référentiels produisent 142 chunks : 43 pour les diplômes, 61 pour le MEPC 2013 et 38 pour la NCF 2017. Le traitement suit cinq étapes :
\begin{itemize}
  \item identifier le document source et son référentiel ;
  \item détecter les titres et la hiérarchie des sections ;
  \item créer un chunk par unité documentaire cohérente ;
  \item vérifier la taille, puis fusionner les fragments de moins de 80 caractères et limiter les chunks à 3\,000 caractères ;
  \item stocker le texte et ses métadonnées dans pgvector pour la recherche.
\end{itemize}
La stratégie de secours par fenêtres glissantes n'a pas été utilisée dans l'index courant.La figure~\ref{fig:implementation_chunking_pipeline} doit être lue comme un contrôle de qualité du contexte injecté dans le RAG. Le découpage ne part pas d'une fenêtre arbitraire, mais de la structure réelle des référentiels : niveaux, domaines, groupes de métiers, diplômes et sections. Cette stratégie limite deux risques. D'abord, elle évite de couper une règle au milieu d'une phrase ou d'une section. Ensuite, elle évite de mélanger dans un même fragment plusieurs règles qui ne répondent pas à la même question.
\begin{figure}[H]
  \centering
  \caption{Pipeline de chunking structurel des référentiels documentaires}
  \label{fig:implementation_chunking_pipeline}
  \includegraphics[width=0.90\textwidth]{chunking.png}
  \source{Nos travaux, schématisation du pipeline de chunking documentaire et d'indexation pgvector.}
\end{figure}
Concrètement, un chunk correspond à une unité interprétable : une partie de nomenclature, un niveau de formation, un groupe métier ou une description réglementaire. Lorsqu'une question porte sur un métier, une compétence ou un niveau \NCF{}, pgvector peut donc récupérer un passage qui a encore un sens seul. C'est ce qui rend le contexte utilisable par l'agent : le LLM ne reçoit pas seulement du texte proche, mais un fragment rattaché à une source, une section et une position documentaire.

La figure~\ref{fig:implementation_chunking_diagnostics} complète cette lecture par trois diagnostics. Premièrement, la taille médiane de 116 tokens indique que les fragments sont assez courts pour être retrouvés et cités sans saturer le contexte. Deuxièmement, la présence de quelques chunks supérieurs à 400 tokens montre que certaines sections des référentiels restent denses ; ces fragments sont acceptables pour préserver le sens, mais ils doivent être surveillés car ils peuvent contenir plusieurs informations. Troisièmement, la couverture moyenne de 42,9\,\% renseigne la part du texte utile conservée après nettoyage et structuration ; elle ne doit pas être interprétée comme une mesure directe de qualité, mais comme un indicateur de densité documentaire.

La comparaison par source montre aussi que les référentiels n'ont pas la même régularité. La NCF présente une cohérence moyenne plus élevée (0,336) que les diplômes (0,268), ce qui signifie que ses sections sont plus homogènes après découpage. Les diplômes demandent donc davantage de vigilance : leurs libellés peuvent être courts, hétérogènes ou très administratifs. Pour le système, cela implique que le retrieval documentaire est plus fiable lorsqu'il interroge des sections bien structurées, et plus fragile lorsqu'il dépend de fragments courts ou descriptivement pauvres. Les mesures complètes figurent en annexe.
\begin{figure}[H]
  \centering
  \caption{Distribution des tailles, sources et couverture du chunking documentaire}
  \label{fig:implementation_chunking_diagnostics}
  \includegraphics[width=0.92\textwidth]{figures/generated/implementation_deep_current/06_chunking_diagnostics.png}
  \source{Nos travaux, diagnostics Python du chunking documentaire.}
\end{figure}

\section{Neo4j : mémoire relationnelle du matching}

Dans le workflow méthodologique, Neo4j intervient après le rappel pgvector et avant le score hybride. Pgvector propose des objets proches dans l'espace sémantique ; Neo4j vérifie ensuite si cette proximité repose sur des relations métier exploitables : compétences possédées, compétences requises, métiers, niveaux \NCF{}, secteurs et localisations. Cette section présente donc la structure du graphe et les précautions nécessaires avant son usage dans le reranking.

\subsection{Schéma, noeuds et relations mobilisés par le matching}

Cette sous-section décrit la partie du graphe réellement utile au matching. L'objectif n'est pas de présenter tous les objets stockés, mais de montrer comment une offre, un candidat et des référentiels deviennent interrogeables par relations.

La figure~\ref{fig:implementation_schema_graphe} montre deux fonctions du graphe : relier les candidats aux offres par les compétences et rattacher ces objets aux référentiels métier et formation. pgvector repère une proximité textuelle ; Neo4j indique par quelles relations cette proximité peut être expliquée.
Le chemin principal est \texttt{Candidat} $\rightarrow$ \texttt{POSSEDE} $\rightarrow$ \texttt{Compétence} $\leftarrow$ \texttt{REQUIERT} $\leftarrow$ \texttt{OffreEmploi}. Il permet de distinguer les compétences couvertes des compétences manquantes. Les liens vers le métier, le niveau \NCF{}, le secteur et la localisation complètent l'explication. Leur fiabilité reste conditionnée par la qualité de l'extraction et de la normalisation.
\begin{figure}[H]
  \centering
  \caption{Schéma logique du graphe de connaissances}
  \label{fig:implementation_schema_graphe}
  \includegraphics[width=0.90\textwidth]{schema.png}
  \source{Nos travaux, diagnostics Neo4j}
\end{figure}
\subsection{Structure du graphe et dépendance aux hubs}

Cette sous-section contrôle si le graphe est exploitable pour le GraphRAG. Dans le workflow, ce diagnostic précède le score hybride : si le graphe est trop fragmenté, Neo4j ne peut pas expliquer les recommandations ; s'il dépend trop de quelques noeuds génériques, il peut au contraire produire des correspondances trop larges.
La figure~\ref{fig:implementation_neo4j_dashboard} regroupe quatre diagnostics complémentaires. Le panneau des labels montre que le graphe est dominé par les candidats (41\,298 noeuds), puis par les groupes de compétences, les offres (13\,957) et les compétences (13\,939). Cette composition est cohérente avec le workflow : relier des profils nombreux à un vivier d'offres à travers des compétences normalisées.
Le deuxième panneau, \og Relations par type \fg{}, indique que les relations \texttt{POSSEDE} dominent avec 221\,413 liens, suivies par \texttt{NECESSITE} (126\,051) et \texttt{REQUIERT} (58\,519). Le graphe est donc bien centré sur la chaîne candidat--compétence--offre, avec un enrichissement par les métiers de référence. C'est utile pour expliquer le \textit{skill gap}, mais cela rend aussi le système sensible à la qualité d'extraction des compétences : une compétence mal extraite crée directement un lien erroné.
\begin{figure}[H]
  \centering
  \caption{Tableau de bord statistique du graphe Neo4j}
  \label{fig:implementation_neo4j_dashboard}
  \includegraphics[width=0.90\textwidth]{figures/generated/implementation_deep_current/14_neo4j_dashboard_reseau.png}
  \source{Nos travaux, diagnostics Neo4j}
\end{figure}
Le troisième panneau, \og Degrés maximaux \fg{}, révèle la présence de hubs. Les niveaux \NCF{} atteignent 10\,261 connexions et certaines compétences 6\,787, alors que le degré médian d'une compétence est de 4. Ces hubs sont utiles pour connecter le graphe, mais ils sont peu discriminants pour recommander une offre précise. Ils ne doivent donc pas être interprétés comme des preuves fortes de compatibilité.

Le quatrième panneau, \og Robustesse \fg{}, teste la dépendance du graphe à ces hubs. Lorsque les 1\,\% de noeuds les plus connectés sont retirés, la composante géante ne conserve plus que 30,1\,\% de sa taille initiale ; après retrait des 5\,\%, elle tombe à 11,3\,\% ; après 10\,\%, elle s'effondre presque totalement. Le résultat est clair : la connectivité du graphe repose fortement sur quelques noeuds génériques. Cette faiblesse ne bloque pas l'usage de Neo4j, mais elle impose de ne pas utiliser le degré brut comme score de pertinence.

Le graphe contient au total 90\,230 noeuds et 550\,542 relations. Sa faible densité ($6{,}76\times10^{-5}$) n'indique donc pas un manque d'information ; elle traduit un schéma spécialisé où une offre est reliée aux entités qui l'expliquent, et non à toutes les entités disponibles. La projection de matching regroupe 72\,516 noeuds et 499\,080 arêtes dans une composante connexe : les chemins candidat--compétence--offre existent, mais leur pertinence dépend de la qualité des relations \texttt{POSSEDE} et \texttt{REQUIERT}. La composition détaillée est reportée en annexe.

La distribution est asymétrique : les niveaux NCF atteignent 10\,261 connexions et certaines compétences 6\,787, alors que le degré médian d'une compétence est de 4. Quelques libellés généraux concentrent donc une grande partie des chemins et sont peu discriminants.
\begin{figure}[H]
  \centering
  \caption{Distribution des degrés dans la projection matching}
  \label{fig:implementation_neo4j_distribution_degres}
  \includegraphics[width=0.90\textwidth]{figures/generated/implementation_deep_current/14_neo4j_distribution_degres.png}
  \source{Nos travaux, diagnostics Neo4j sur la projection des relations utilisées pour le matching.}
\end{figure}
Le score Neo4j utilise donc la proportion de compétences requises couvertes, et non le degré brut des noeuds. Une compétence fréquente n'améliore pas automatiquement une recommandation ; elle ne compte que si elle est exigée par l'offre et associée au candidat. Cette règle protège le reranking contre le bruit de popularité et prépare la section suivante, où Neo4j devient un signal pondéré du score hybride.

\section{Score hybride et reranking des recommandations}

Le moteur combine le rappel sémantique de pgvector et la couverture de compétences calculée dans Neo4j. Il réordonne les offres avant leur transmission au module de génération.

\subsection{Formalisation du score hybride}

Dans la version calibrée, le score combine deux signaux :

\[
S_{\text{hybride}} =
\alpha S_{\text{pgvector}} + \beta S_{\text{Neo4j}},
\quad \alpha+\beta=1
\]

\noindent où \texttt{score\_sem} mesure la similarité pgvector et \texttt{taux\_match} la part des compétences requises couvertes dans Neo4j. Le niveau NCF, le secteur et la localisation restent disponibles pour l'explication, mais ne reçoivent pas de poids autonome dans cette calibration.

Sur 690 couples candidat--offre, le score pgvector est continu et centré autour de 0,52, tandis que 31,30\,\% des scores Neo4j sont nuls. Le premier panneau montre donc une proximité textuelle graduelle ; le second montre un signal relationnel plus sélectif. pgvector distingue des degrés de ressemblance entre presque tous les couples ; Neo4j écarte plus nettement ceux pour lesquels aucune compétence requise n'est couverte.
\begin{figure}[H]
  \centering
  \caption{Distribution comparée des scores pgvector et Neo4j utilisés dans le reranking}
  \label{fig:implementation_distribution_scores_hybrides}
  \includegraphics[width=0.90\textwidth]{figures/generated/implementation_deep_current/13_distribution_scores_pgvector_neo4j.png}
  \source{Nos travaux, diagnostics PostgreSQL--pgvector, à partir des embeddings}
\end{figure}
Avec 500 essais Optuna, l'objectif NDCG@10 retient $\alpha=0$ et $\beta=1$ : ce résultat de bord est inhabituel et ne doit pas être interprété comme l'inutilité de pgvector. Le vivier a déjà été présélectionné par pgvector ; l'optimisation ne compare donc que des offres sémantiquement plausibles. Dans ce sous-ensemble, la couverture relationnelle peut suffire à ordonner le top~10 selon le proxy retenu. Le résultat peut aussi refléter un alignement partiel entre ce proxy et les signaux de couverture utilisés par Neo4j. L'objectif MRR@10--Precision@10 retient plutôt 0,6615 pour pgvector et 0,3385 pour Neo4j, car il valorise davantage le premier résultat pertinent. Les poids sont donc propres à l'objectif et à l'échantillon de calibration ; ils doivent être confrontés à une analyse de sensibilité et à des jugements indépendants avant d'être fixés en production.
\begin{table}[H]
  \centering
  \caption{Résultats de calibration des poids du score hybride}
  \label{tab:implementation_moteur_hybride_score}
  \tableStyleStart
  \normalsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1.16}
  \begin{tabularx}{0.98\textwidth}{>{\raggedright\arraybackslash}X
      >{\raggedright\arraybackslash}p{2.55cm}
      >{\centering\arraybackslash}p{1.75cm}
      >{\centering\arraybackslash}p{1.65cm}
      >{\centering\arraybackslash}p{2.05cm}}
    \rowcolor{darkblue}
    \tabheader{Objectif optimisé} &
    \tabheader{Outil} &
    \tabheader{$\alpha$ Sem} &
    \tabheader{$\beta$ Neo4j} &
    \tabheader{NDCG@10} \\
    \midrule
    NDCG@10 & Neo4j seul & 0,0000 & 1,0000 & 0,9697 \\
    \makecell[l]{$(\text{NDCG@10}+\text{Recall@10}$\\$+\text{Precision@10})/3$} & Neo4j seul & 0,0000 & 1,0000 & 0,9697 \\
    \makecell[l]{$(\text{MRR@10}+\text{Precision@10})/2$} & Mixte Optuna & 0,6615 & 0,3385 & 0,9674 \\
    \bottomrule
  \end{tabularx}
  \tableStyleEnd
  \source{Nos travaux, traitements Python de nettoyage et de diagnostic, à partir des données collectées du projet.}
\end{table}
\subsection{Effet du reranking hybride}

Neo4j ne relance pas la recherche : il réordonne le vivier pgvector selon la couverture de compétences. Les déplacements de rang et l'ablation avec ou sans graphe sont analysés au chapitre~4. Le tableau~\ref{tab:implementation_moteur_hybride_scores} en annexe conserve le résumé numérique.

\section{Orchestration agentique du système par LangGraph}

Cette dernière brique transforme les composants précédents en agent IA outillé. Dans ce mémoire, l'agent n'est pas un simple chatbot qui répond directement à partir d'un prompt. Il suit une boucle contrôlée : comprendre la demande, choisir les outils utiles, exécuter les recherches, observer les résultats, construire un contexte, générer une réponse, puis vérifier si cette réponse est suffisamment appuyée par les preuves.

LangGraph sert à représenter cette boucle sous forme de graphe d'états. L'état partagé contient la requête, le cas d'usage, les outils planifiés, leurs résultats, le contexte construit, la réponse, la décision du critic et les traces d'exécution. L'agent ne remplace donc pas pgvector, Neo4j ou le score hybride ; il décide quand les mobiliser et comment combiner leurs sorties pour répondre.

\begin{figure}[H]
  \centering
  \caption{Workflow agentique orchestré dans LangGraph Studio}
  \label{fig:implementation_langgraph_orchestration}
  \includegraphics[width=0.94\textwidth]{langchaindev.png}
  \source{Nos travaux, visualisation LangGraph Studio du workflow implémenté.}
\end{figure}

La figure~\ref{fig:implementation_langgraph_orchestration} décrit le comportement de cet agent IA. Le noeud \texttt{analyse\_request} joue le rôle de perception : il transforme une question libre en intention exploitable. \texttt{plan\_tools} correspond à la planification : l'agent choisit les outils nécessaires au lieu d'appeler toutes les bases sans distinction. \texttt{execute\_tools} représente l'action : l'agent interroge pgvector, Neo4j ou le moteur hybride. \texttt{build\_context} correspond à l'observation organisée : les résultats bruts sont regroupés en preuves utilisables. Enfin, \texttt{generate\_final\_answer} produit la réponse à partir du contexte construit.

Les flèches de retour vers \texttt{expand\_context} sont importantes. Elles montrent que l'agent peut réviser sa trajectoire lorsque les preuves sont insuffisantes : il élargit alors la recherche, reconstruit le contexte et régénère la réponse. L'agent reste cependant borné par le workflow ; il ne crée pas librement de nouvelles actions et ne modifie pas les bases. Cette contrainte est nécessaire pour garder un système défendable.

\subsection{Perception de la requête et planification des outils}

Le noeud \texttt{analyse\_request} extrait la question, recherche un identifiant candidat et classe la demande dans un cas d'usage : diagnostic, recommandation, analyse de \textit{skill gap}, consultation d'un référentiel, interrogation du graphe, orientation métier, synthèse globale ou recherche générale. Le noeud \texttt{plan\_tools} associe ensuite ce cas aux outils nécessaires.

Cette étape correspond à la partie décisionnelle de l'agent. Une question sur un référentiel mobilise les chunks pgvector ; une question relationnelle mobilise Neo4j ; une recommandation candidat--offre appelle le moteur hybride ; une demande de diagnostic vérifie plutôt l'état des services. Le registre contient six outils principaux : diagnostic, recherche sémantique, recherche documentaire, interrogation Neo4j, recommandation hybride et synthèse globale du graphe.

Cette planification évite deux erreurs fréquentes dans les applications \LLM{}. La première consiste à demander au modèle de répondre sans preuve. La seconde consiste à appeler tous les outils à chaque question, ce qui alourdit le système et produit du bruit. Ici, l'agent choisit le chemin le plus pertinent selon l'intention détectée.

\subsection{Action, observation et construction du contexte}

\texttt{execute\_tools} appelle les outils planifiés et conserve chaque résultat ou erreur dans l'état. \texttt{build\_context} rassemble ensuite les sorties hétérogènes dans une structure commune : offres candidates, compétences couvertes ou manquantes, lignes Neo4j, résultats sémantiques, fragments documentaires et état des services.

Dans une logique d'agent IA, cette étape correspond au couple action--observation. L'action est l'appel aux outils ; l'observation est la transformation des sorties en éléments interprétables. Par exemple, un résultat pgvector donne une proximité sémantique, tandis qu'un résultat Neo4j donne un chemin relationnel ou une couverture de compétences. Ces deux observations ne sont pas équivalentes ; elles doivent être rapprochées avant la génération.

Cette normalisation sépare la récupération de la rédaction. Le \LLM{} ne consulte pas directement les bases et ne choisit pas librement les données à utiliser ; il reçoit un contexte déjà récupéré et structuré. Les erreurs d'un outil restent également traçables sans supprimer les résultats valides produits par les autres outils. C'est ce qui permet de justifier une réponse : l'agent peut indiquer les offres trouvées, les compétences couvertes, les compétences manquantes et les sources documentaires mobilisées.

\subsection{Génération contrôlée et boucle de révision}

\texttt{generate\_final\_answer} produit une réponse française à partir du contexte et impose la citation des identifiants ou sources disponibles. Si OpenRouter est indisponible, le système restitue les données récupérées sous une forme structurée plutôt que d'inventer une réponse.

Le critic joue le rôle de contrôle interne de l'agent. Il ne vérifie pas toute la vérité métier de la réponse ; il vérifie si la réponse est suffisamment ancrée dans le contexte récupéré. Une décision \texttt{accept} termine le workflow. Une décision \texttt{revise} active \texttt{expand\_context}, augmente le top-$K$ dans la limite de 10 et ajoute les recherches pgvector ou Neo4j qui n'avaient pas encore été exécutées. Le contexte est alors reconstruit et la réponse régénérée.

Cette boucle donne au système un comportement agentique : il ne répond pas seulement, il peut constater que son contexte est insuffisant et demander plus de preuves avant de finaliser. La boucle reste limitée à une seule révision afin d'éviter une exécution indéfinie. Le chapitre~4 présente la calibration du seuil utilisé par ce contrôle.

\section*{Conclusion}

Le chapitre a matérialisé la chaîne suivante : données normalisées, embeddings spécialisés, rappel pgvector, contrôle Neo4j, reranking hybride et orchestration LangGraph. Chaque brique remplit une fonction distincte, tandis que l'orchestrateur assure leur enchaînement et la traçabilité des décisions. Le résultat reste dépendant de la qualité des données, des relations de compétences et du contexte récupéré ; le chapitre~4 mesure l'effet de ces choix sur le classement et la génération.
